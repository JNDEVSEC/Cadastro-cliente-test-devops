name: Security Scan

on:
  workflow_call:
    inputs:
      project-path:
        required: false
        default: '.'
        type: string

  push:
    branches: ["main"]

  pull_request:
    branches: ["main"]

  schedule:
    - cron: '16 14 * * 2'

  workflow_dispatch:

permissions:
  contents: read
  security-events: write

# ✅ OPT-OUT OFICIAL PARA EVITAR ANNOTATION / EXIT CODE 1
env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.project-path || '.' }}
        shell: bash

    steps:
      # =========================
      # CHECKOUT
      # =========================
      - name: Checkout repository
        uses: actions/checkout@v4

      # =========================
      # PYTHON
      # =========================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # =========================
      # SYSTEM DEPS
      # =========================
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      # =========================
      # PYTHON DEPS
      # =========================
      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install reportlab PyPDF2 semgrep

      # =========================
      # SEMGREP
      # =========================
      - name: Run Semgrep JSON
        run: semgrep scan --config auto --json --output semgrep.json || true

      - name: Run Semgrep SARIF
        run: semgrep scan --config auto --sarif --output semgrep.sarif || true

      - name: Convert JSON to CSV
        run: |
          jq -r '
            ["Rule ID","Severity","File","Line","Message"],
            (.results[]? | [
              .check_id,
              .extra.severity,
              .path,
              .start.line,
              .extra.message
            ]) | @csv
          ' semgrep.json > semgrep.csv

      - name: Build flat JSON
        run: |
          jq '{
            results: [
              .results[]? | {
                rule_id: .check_id,
                severity: .extra.severity,
                file: .path,
                line: .start.line,
                message: .extra.message
              }
            ]
          }' semgrep.json > semgrep.report.json

      - name: Upload Semgrep SARIF
        if: ${{ hashFiles('semgrep.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: semgrep.sarif

      # =========================
      # TRIVY – FILESYSTEM
      # =========================
      - name: Run Trivy (filesystem)
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: '.'
          format: json
          output: trivy-results.json
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
        env:
          TRIVY_SCANNERS: 'vuln,secret,misconfig'

      - name: Checkout security templates
        uses: actions/checkout@v4
        with:
          repository: JNDEVSEC/security-templates
          ref: v1
          path: .security-templates
          token: ${{ secrets.SECURITY_TEMPLATES_TOKEN }}

      - name: Run custom security review
        run: |
          python .security-templates/scripts/security_review.py \
            --root . \
            --json-out custom-review.json \
            --sarif-out custom-review.sarif \
            --fail-on HIGH

      - name: Upload Custom Review SARIF
        if: ${{ hashFiles('custom-review.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: custom-review.sarif

      # =========================
      # PDF
      # =========================
      - name: Generate PDF Report
        run: python scripts/generate_report.py

      # =========================
      # ARTEFATOS
      # =========================
      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            semgrep.json
            semgrep.csv
            semgrep.report.json
            semgrep.sarif
            trivy-results.json
            custom-review.json
            custom-review.sarif
            security-report.pdf
          if-no-files-found: ignore
