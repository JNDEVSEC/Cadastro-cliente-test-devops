name: Security Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  security:
    uses: JNDEVSEC/security-templates/.github/workflows/security-scan.yml@v2
    secrets: inherit

