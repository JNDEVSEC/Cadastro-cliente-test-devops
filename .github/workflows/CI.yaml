name: Security Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  security:
    uses: JNDEVSEC/security-templates/.github/workflows/security_scan.yml@main
    secrets: inherit

