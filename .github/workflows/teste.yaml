name: Teste – Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: uses: JNDEVSEC/security-templates/.github/workflows/security_scan.yml@main
    secrets: inherit
    with:
      project-path: '.'
