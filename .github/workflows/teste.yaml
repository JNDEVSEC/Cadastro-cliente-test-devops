name: Teste – Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: JNDEVSEC/security-templates/.github/workflows/security_scan.yml@v1.0.0
    secrets: inherit
    with:
      project-path: '.'
